[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n#include <ctype.h>\r\n\r\nint countWords(char str[]) {\r\n    int wordCount = 0;\r\n    int inWord = 0;  // Flag to track if we're inside a word\r\n    \r\n    for (int i = 0; str[i] != '\\0'; i++) {\r\n        if (isspace(str[i])) {\r\n            inWord = 0;  // Found a space, not in word\r\n        } else if (!inWord) {\r\n            inWord = 1;  // Start of a new word\r\n            wordCount++;\r\n        }\r\n    }\r\n    \r\n    return wordCount;\r\n}\r\n\r\nint main() {\r\n    char str[1000];\r\n    \r\n    // Read input string\r\n    fgets(str, sizeof(str), stdin);\r\n    \r\n    // Remove trailing newline if present\r\n    str[strcspn(str, \"\\n\")] = '\\0';\r\n    \r\n    // Count and print number of words\r\n    printf(\"%d\\n\", countWords(str));\r\n    \r\n    return 0;\r\n}","language":"c","time":"2025-04-29T03:12:33.977Z","isCorrect":true}]
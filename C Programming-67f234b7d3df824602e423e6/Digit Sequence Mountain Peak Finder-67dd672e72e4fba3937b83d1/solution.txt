[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n#include <stdbool.h>\r\n\r\nint mountainPeak(int N) {\r\n    char numStr[20];\r\n    sprintf(numStr, \"%d\", N);\r\n    int len = strlen(numStr);\r\n\r\n    if (len < 3) {\r\n        return -1;\r\n    }\r\n\r\n    int peakIndex = -1;\r\n    for (int i = 1; i < len - 1; i++) {\r\n        if (numStr[i] > numStr[i - 1] && numStr[i] > numStr[i + 1]) {\r\n            if (peakIndex == -1) {\r\n                peakIndex = i;\r\n            } else {\r\n                return -1; // More than one peak\r\n            }\r\n        }\r\n    }\r\n\r\n    if (peakIndex == -1) {\r\n        return -1; // No peak found\r\n    }\r\n\r\n    // Check if strictly increasing before peak\r\n    for (int i = 1; i <= peakIndex; i++) {\r\n        if (numStr[i] <= numStr[i - 1]) {\r\n            return -1;\r\n        }\r\n    }\r\n\r\n    // Check if strictly decreasing after peak\r\n    for (int i = peakIndex + 1; i < len; i++) {\r\n        if (numStr[i] >= numStr[i - 1]) {\r\n            return -1;\r\n        }\r\n    }\r\n\r\n    return numStr[peakIndex] - '0';\r\n}","language":"c","time":"2025-07-26T18:00:51.939Z","isCorrect":true}]
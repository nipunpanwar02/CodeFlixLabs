[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\n// Define the Student structure\r\nstruct Student {\r\n    int rollNumber;\r\n    char name[100];\r\n    float marks;\r\n};\r\n\r\nint main() {\r\n    int N;\r\n    scanf(\"%d\", &N);\r\n    \r\n    // Create an array of Student structures\r\n    struct Student students[N];\r\n    \r\n    // Input student details\r\n    for (int i = 0; i < N; i++) {\r\n        scanf(\"%d %s %f\", &students[i].rollNumber, students[i].name, &students[i].marks);\r\n    }\r\n    \r\n    // Find the student with minimum marks\r\n    int minIndex = 0;\r\n    for (int i = 1; i < N; i++) {\r\n        if (students[i].marks < students[minIndex].marks) {\r\n            minIndex = i;\r\n        }\r\n    }\r\n    \r\n    // Display the student with minimum marks in the required format\r\n    printf(\"Student with Minimum Marks: Roll Number: %d, Name: %s, Marks: %.2f\\n\",\r\n           students[minIndex].rollNumber, students[minIndex].name, students[minIndex].marks);\r\n    \r\n    return 0;\r\n}","language":"c","time":"2025-04-29T02:45:18.637Z","isCorrect":true}]
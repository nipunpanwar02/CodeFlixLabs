[{"code":"#include <stdio.h>\r\n\r\nvoid trackPlayerRanks(int ranked[], int n, int player[], int m, int result[]) {\r\n    int denseRank[n];\r\n    int rank = 1;\r\n    denseRank[0] = 1;\r\n\r\n    for (int i = 1; i < n; i++) {\r\n        if (ranked[i] == ranked[i - 1]) {\r\n            denseRank[i] = rank;\r\n        } else {\r\n            rank++;\r\n            denseRank[i] = rank;\r\n        }\r\n    }\r\n\r\n    for (int i = 0; i < m; i++) {\r\n        int playerScore = player[i];\r\n        int currentRank = n + 1;\r\n        for (int j = 0; j < n; j++) {\r\n            if (playerScore >= ranked[j]) {\r\n                currentRank = denseRank[j];\r\n                \r\n                int k = j;\r\n                while(k >= 0 && playerScore >= ranked[k]) {\r\n                    k--;\r\n                }\r\n                \r\n                if(k == -1){\r\n                    currentRank = 1;\r\n                } else {\r\n                  currentRank = denseRank[k+1];\r\n                }\r\n\r\n                break;\r\n            } else {\r\n                currentRank = denseRank[n-1] + 1;\r\n            }\r\n           \r\n        }\r\n        if(playerScore > ranked[0]){\r\n            currentRank = 1;\r\n        } else if (playerScore < ranked[n-1]){\r\n             currentRank = denseRank[n-1] + 1;\r\n        }\r\n        result[i] = currentRank;\r\n    }\r\n}\r\n","language":"c","time":"2025-07-25T17:36:57.550Z","isCorrect":true}]
[{"code":"#include <stdio.h>\r\n#include <ctype.h>  // For tolower() function\r\n\r\nint main() {\r\n    char str[1000];\r\n    int vowelCount = 0;\r\n    \r\n    // Read input string\r\n    fgets(str, sizeof(str), stdin);\r\n    \r\n    // Count vowels\r\n    for (int i = 0; str[i] != '\\0'; i++) {\r\n        char c = tolower(str[i]);  // Convert to lowercase for easy comparison\r\n        if (c == 'a' || c == 'e' || c == 'i' || c == 'o' || c == 'u') {\r\n            vowelCount++;\r\n        }\r\n    }\r\n    \r\n    // Output the result\r\n    printf(\"%d\\n\", vowelCount);\r\n    \r\n    return 0;\r\n}","language":"c","time":"2025-04-29T03:09:19.840Z","isCorrect":true}]
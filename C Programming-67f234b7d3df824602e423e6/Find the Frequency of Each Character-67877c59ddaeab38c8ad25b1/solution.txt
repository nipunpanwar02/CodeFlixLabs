[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[100];\r\n    int freq[256] = {0};  // Assuming ASCII characters\r\n    int i;\r\n\r\n    // Input the string\r\n    printf(\"Enter a string: \");\r\n    fgets(str, sizeof(str), stdin); // Use fgets to safely read input\r\n    str[strcspn(str, \"\")] = 0; // Remove trailing newline\r\n\r\n    // Calculate the frequency of each character\r\n    for (i = 0; str[i] != '\\0'; i++) {\r\n        freq[(int)str[i]]++;\r\n    }\r\n\r\n    // Print the frequency of each character\r\n    for (i = 0; i < 256; i++) {\r\n        if (freq[i] != 0) {\r\n            printf(\"%c: %d\", (char)i, freq[i]);\r\n        }\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-07-16T17:33:04.049Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\nint main() {\r\n    char str[100];\r\n    int freq[256] = {0};  // Assuming ASCII characters\r\n    int i;\r\n\r\n    // Input the string\r\n    fgets(str, sizeof(str), stdin); // Use fgets to safely read input\r\n    str[strcspn(str, \"\\n\")] = 0;     // Remove trailing newline\r\n\r\n    // Calculate the frequency of each character\r\n    for (i = 0; str[i] != '\\0'; i++) {\r\n        freq[(int)str[i]]++;\r\n    }\r\n\r\n    // Print the frequency of each character\r\n    for (i = 0; i < 256; i++) {\r\n        if (freq[i] != 0) {\r\n            printf(\"%c: %d\\n\", (char)i, freq[i]);\r\n        }\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-07-16T17:34:14.789Z","isCorrect":false}]
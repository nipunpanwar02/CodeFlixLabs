[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n#include <ctype.h>\r\n#include <stdbool.h>\r\n\r\nint main() {\r\n    char str[1000];\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    // Remove trailing newline if present\r\n    if (str[strlen(str) - 1] == '0') {\r\n        str[strlen(str) - 1] = '\\0';\r\n    }\r\n\r\n    char cleanStr[1000];\r\n    int j = 0;\r\n    for (int i = 0; str[i] != '\\0'; i++) {\r\n        if (!isspace(str[i])) {\r\n            cleanStr[j++] = str[i];\r\n        }\r\n    }\r\n    cleanStr[j] = '\\0';\r\n\r\n    int left = 0;\r\n    int right = strlen(cleanStr) - 1;\r\n    bool isPalindrome = true;\r\n\r\n    while (left < right) {\r\n        if (cleanStr[left] != cleanStr[right]) {\r\n            isPalindrome = false;\r\n            break;\r\n        }\r\n        left++;\r\n        right--;\r\n    }\r\n\r\n    if (isPalindrome) {\r\n        printf(\"Yes\");\r\n    } else {\r\n        printf(\"No\");\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-07-16T17:39:35.830Z","isCorrect":false},{"code":"#include <stdio.h>\r\n#include <string.h>\r\n#include <ctype.h>\r\n#include <stdbool.h>\r\n\r\nint main() {\r\n    char str[1000];\r\n    fgets(str, sizeof(str), stdin);\r\n\r\n    // Remove trailing newline if present\r\n    str[strcspn(str, \"\\n\")] = '\\0';\r\n\r\n    char cleanStr[1000];\r\n    int j = 0;\r\n\r\n    for (int i = 0; str[i] != '\\0'; i++) {\r\n        if (isalnum(str[i])) { // Keep only letters and digits\r\n            cleanStr[j++] = tolower(str[i]); // Convert to lowercase\r\n        }\r\n    }\r\n    cleanStr[j] = '\\0';\r\n\r\n    int left = 0;\r\n    int right = strlen(cleanStr) - 1;\r\n    bool isPalindrome = true;\r\n\r\n    while (left < right) {\r\n        if (cleanStr[left] != cleanStr[right]) {\r\n            isPalindrome = false;\r\n            break;\r\n        }\r\n        left++;\r\n        right--;\r\n    }\r\n\r\n    if (isPalindrome) {\r\n        printf(\"Yes\");\r\n    } else {\r\n        printf(\"No\");\r\n    }\r\n\r\n    return 0;\r\n}\r\n","language":"c","time":"2025-07-16T17:40:30.568Z","isCorrect":true}]
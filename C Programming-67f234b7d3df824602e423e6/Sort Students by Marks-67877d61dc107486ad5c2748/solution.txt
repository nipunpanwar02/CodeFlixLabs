[{"code":"#include <stdio.h>\r\n#include <string.h>\r\n\r\n// Define the Student structure\r\nstruct Student {\r\n    int rollNumber;\r\n    char name[100];\r\n    float marks;\r\n};\r\n\r\nint main() {\r\n    int N;\r\n    scanf(\"%d\", &N);\r\n    \r\n    // Create an array of Student structures\r\n    struct Student students[N];\r\n    \r\n    // Input student details\r\n    for (int i = 0; i < N; i++) {\r\n        scanf(\"%d %s %f\", &students[i].rollNumber, students[i].name, &students[i].marks);\r\n    }\r\n    \r\n    // Sort students in descending order of marks using bubble sort\r\n    for (int i = 0; i < N - 1; i++) {\r\n        for (int j = 0; j < N - i - 1; j++) {\r\n            if (students[j].marks < students[j + 1].marks) {\r\n                // Swap the students\r\n                struct Student temp = students[j];\r\n                students[j] = students[j + 1];\r\n                students[j + 1] = temp;\r\n            }\r\n        }\r\n    }\r\n    \r\n    // Display the sorted students in the exact required format\r\n    for (int i = 0; i < N; i++) {\r\n        printf(\"Roll Number: %d, Name: %s, Marks: %.2f\",\r\n               students[i].rollNumber, students[i].name, students[i].marks);\r\n        if (i < N - 1) {\r\n            printf(\"\\n\");  // Only print newline if not the last student\r\n        }\r\n    }\r\n    \r\n    return 0;\r\n}","language":"c","time":"2025-04-29T02:47:23.868Z","isCorrect":true}]